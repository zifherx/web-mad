"use server";

import { UpdateTestimonialInput } from "@/lib/validations/testimonial.validation";

export async function updateTestimonial(
  id: string,
  data: UpdateTestimonialInput
) {
  try {
  } catch (err) {}
}
