import { IconProp } from "@/types";

export interface SubmenuFooter {
  id: number;
  titleOption: string;
  linkOption: string;
}

export interface LinkIcon {
  text: string;
  icon: IconProp;
  href: string;
}
